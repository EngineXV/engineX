# Examples

Runnable agent templates live in [`templates/`](templates/).

```bash
./engine validate examples/templates/contract_review
./engine run examples/templates/contract_review --tui
```

Copy a template into `exports/`, customize goals and nodes, then run it with `./engine run`.

See [`templates/README.md`](templates/README.md) for the full list.
