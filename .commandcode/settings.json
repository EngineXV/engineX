{
  "permissions": {
    "allow": [
      "Shell(python -c import pydantic; print(pydantic.VERSION) 2 > /dev/null)",
      "Shell(python -m pytest tests/test_shared_state_persistence.py -v 2 >& 1)",
      "Shell(cd /home/vidorc/projects/engineX/core && python - <<'EOF' 2>&1 | tail -30 import asyncio, tempfile, json from pathlib import Path from datetime import datetime from engine.runtime.shared_state import IsolationLevel, SharedStateManager from engine.schemas.session_state import SessionState, SessionTimestamps, SessionStatus from engine.storage.session_store import SessionStore SESSION_ID = \"session_dbg\" async def main(): with tempfile.TemporaryDirectory() as tmp: store = SessionStore(Path(tmp)) sid = SESSION_ID state = SessionState( session_id=sid, goal_id=\"g\", stream_id=\"s\", status=SessionStatus.ACTIVE, timestamps=SessionTimestamps(started_at=datetime.now().isoformat(), updated_at=datetime.now().isoformat()), ) p = store.get_state_path(sid); p.parent.mkdir(parents=True, exist_ok=True) p.write_text(state.model_dump_json(indent=2), encoding=\"utf-8\") m = SharedStateManager(session_store=store, session_id=sid) print(\"queue after init:\", m._persist_queue) await m.write(\"k\", \"v\", \"exec-1\", \"stream-1\", IsolationLevel.SHARED, scope=\"global\") print(\"queue after write:\", m._persist_queue) print(\"thread alive:\", m._persist_thread.is_alive() if m._persist_thread else None) try: await asyncio.wait_for(m.flush(), timeout=5) print(\"flush ok\") except Exception as e: print(\"flush error:\", type(e).__name__, e) raw = json.loads(p.read_text(encoding=\"utf-8\")) print(\"shared_state on disk:\", raw.get(\"shared_state\")) asyncio.run(main()) EOF)",
      "Shell(python -m pytest tests/test_shared_state_persistence.py -W error::pytest.PytestUnraisableExceptionWarning 2 >& 1)",
      "Shell(python -m pytest tests/test_shared_state_persistence.py -W error::pytest.PytestUnraisableExceptionWarning -v 2 >& 1)",
      "Shell(cd /home/vidorc/projects/engineX/core && python - <<'EOF' 2>&1 | tail -25 import asyncio, tempfile, threading from pathlib import Path from datetime import datetime from engine.runtime.shared_state import IsolationLevel, SharedStateManager from engine.schemas.session_state import SessionState, SessionTimestamps, SessionStatus from engine.storage.session_store import SessionStore async def main(): with tempfile.TemporaryDirectory() as tmp: store = SessionStore(Path(tmp)) sid = \"session_dbg2\" state = SessionState( session_id=sid, goal_id=\"g\", stream_id=\"s\", status=SessionStatus.ACTIVE, timestamps=SessionTimestamps(started_at=datetime.now().isoformat(), updated_at=datetime.now().isoformat()), ) p = store.get_state_path(sid); p.parent.mkdir(parents=True, exist_ok=True) p.write_text(state.model_dump_json(indent=2), encoding=\"utf-8\") m = SharedStateManager(session_store=store, session_id=sid) await m.write(\"k\", \"v\", \"exec-1\", \"stream-1\", IsolationLevel.SHARED, scope=\"global\") await m.flush() print(\"thread:\", m._persist_thread, \"alive:\", m._persist_thread.is_alive() if m._persist_thread else None) print(\"worker task:\", m._persist_worker_task, \"done:\", m._persist_worker_task.done() if m._persist_worker_task else None) m.close() print(\"after close - thread:\", m._persist_thread) await asyncio.sleep(0.1) print(\"done\") asyncio.run(main()) EOF)",
      "Shell(python -m pytest tests/test_agent_runtime.py tests/test_execution_stream.py tests/test_storage_migrate.py -q 2 >& 1)",
      "Shell(python -m pytest tests/ -q 2 >& 1)"
    ],
    "deny": [],
    "defaultMode": "default"
  }
}